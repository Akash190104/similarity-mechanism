name: "Similarity Testing on Prisoners Dilemma"

# Component references (relative to configs/)
game_config: games/prisoners_dilemma.yaml
mechanism_config: mechanisms/similarity.yaml
agents_config: agents/two_models.yaml
evaluation_config: evaluation/default_evaluation.yaml

# Experiment-level settings
concurrency:
  # Global max workers for LLM API calls (default threads in ThreadPoolExecutor)
  max_workers: 3
  # Parallel tournament matchups (mechanism-level parallelism)
  tournament_workers: 8
