name: "Public Goods Similarity Tournament"

# Component references (relative to configs/)
game_config: games/public_goods.yaml
mechanism_config: mechanisms/similarity_sweep.yaml
agents_config: agents/cheap_llms_3.yaml
evaluation_config: evaluation/default_evaluation.yaml

# Experiment-level settings
concurrency:
  max_workers: 25
  tournament_workers: 25
